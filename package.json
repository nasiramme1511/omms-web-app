{
  "name": "omms-web-app",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "npm run install:all && cd frontend && npm run build && cd ../backend && npm run build",
    "start": "cd backend && npx prisma db push --accept-data-loss && npx tsc && node dist/src/index.js"
  }
}
